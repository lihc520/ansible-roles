[Unit]
Description=nginx
After=network.target

[Service]
Type=forking
ExecStart={{ nginx_root_dir }}/sbin/nginx
ExecReload={{ nginx_root_dir }}/sbin/nginx -s reload
ExecStop={{ nginx_root_dir }}/sbin/nginx -s quit
PrivateTmp=true

[Install]
WantedBy=multi-user.target